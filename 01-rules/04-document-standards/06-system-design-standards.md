# 系统设计文档标准
> 📌 **适用场景：生成系统总体设计文档时必须遵守以下规范，其他技术方案不强制使用此模板**

---

## 🔴 HARD RULE（最重要，Blocking Gate）

**生成系统设计文档前，第一步必须读取标准模板文件并严格遵循其完整章节结构。**

```
模板文件: 09-templates/系统设计文档模板.md
         物理模型: 09-templates/系统物理模型设计文档模版.md（Markdown渲染模板）
                  09-templates/系统物理模型设计文档-数据格式.json（结构化数据schema）
         接口设计: 09-templates/系统接口设计文档模版.md

安装后路径: ~/.claude/templates/

规则:
1. ⛔ 禁止凭记忆生成系统设计文档，必须先 Read 模板文件
2. ⛔ 禁止只遵循主章节、忽略子章节
3. ⛔ 禁止缺省任何一个子章节
4. ✅ 生成后必须逐章节核对，缺失任何子章节视为不合格
```

---

## 🔴 强制执行规则
1. **必须按 7 大章节结构输出，禁止缺省章节**
2. **物理模型设计必须独立成文，遵循两阶段流程（见下方 HARD RULE）**
   - 阶段1：按 09-templates/系统物理模型设计文档-数据格式.json 生成结构化 JSON
   - 阶段2：按 09-templates/系统物理模型设计文档模版.md 渲染为 Markdown
   - 字段、其他信息、索引、数据量级、归档规则统一在表格内呈现
3. **接口设计必须独立成文，遵循《系统接口设计文档模板》**
   - 每个接口包含：服务端点、请求参数、返回码说明、数据字典、范例请求 5 个子项
4. **每个架构设计章节必须包含配套架构图（使用 fireworks-tech-graph 工具生成）**
   - 技术架构图必须包含：前端层、后端服务层、中间件层、基础底座层
5. **设计文档必须包含完整的质量属性识别和量化指标**
6. **生成前必须先 Read 对应的模板文件，严格对齐其所有子章节**

---

## 📋 系统设计文档标准章节结构
生成系统设计文档必须包含以下全部章节结构：

---

### 📌 文档头
```markdown
# [系统名称] 系统设计文档

| 文档编号 | SDD-XXXX-001 | 文档密级 | 内部 |
| --- | --- | --- | --- |
| 归属项目 | | 归属部门 | |
| 编写人 | | 编写日期 | YYYY-MM-DD |
| 审核人 | | 审核日期 | |
```

---

## 一、引言
必须包含以下**2个子章节**：

| 子章节 | 内容要求 |
|-------|---------|
| 1.1 术语和缩略语 | 术语缩写、全称、说明（表格形式，可引用通用文档库） |
| 1.2 参考资料 | 文档名称 + 链接/位置（表格形式，至少3项参考） |

---

## 二、概述
必须包含以下**4个子章节**：

| 子章节 | 内容要求 |
|-------|---------|
| 2.1 开发环境 | 操作系统、IDE、运行环境、数据库、缓存等版本信息（表格形式） |
| 2.2 运行环境 | SE环境、ST环境、压测环境、UAT环境、PRE环境、生产环境的配置（含k8s、docker、https说明，生产环境使用分行展示操作系统、运行环境等） |
| 2.3 限制和约束 | 技术架构规范、业务约束、合规要求、第三方依赖限制 |
| 2.4 质量属性识别 | 类别、质量属性、指标、备注（至少3项量化指标） |

---

## 三、架构设计
必须包含以下**4个子章节**，每个子章节还需包含其内部细分：

| 子章节 | 内容要求 |
|-------|---------|
| 3.1 系统架构 | 3.1.1 架构理念（系统定位、核心原则）<br>3.1.2 系统职责（核心边界、上下游依赖）<br>3.1.3 子系统定位（子系统划分、职责矩阵）<br>3.1.4 设计原则（根据当前系统特性补充，**不使用固定标题**，如跑批原则、数据分发原则、交互原则等）<br>3.1.5 系统上下文（系统交互图 + 交互频率矩阵表）<br>3.1.6 子系统清单（子系统编码、名称、职责定位、应用类型、备注） |
| 3.2 部署架构 | 3.2.1 逻辑部署图（架构图 + 说明）<br>3.2.2 物理部署图（**架构图不可缺失**）<br>3.2.2.1 部署单元划分（序号、运行子系统、部署单元、描述、类别）<br>3.2.2.2 中间件资源使用说明（中间件类型、用途/目的、使用原因说明、备注）<br>3.2.2.3 外联访问说明（外部应用/机构、子系统名、用途/目的、备注）<br>3.2.3 高可用设计（限流降级、灾备切换、故障转移）<br>3.2.4 可扩展设计（业务扩展性、多集群扩展性、代码扩展性） |
| 3.3 技术架构 | 3.3.1 技术架构图（**分层架构图必须包含：前端层、后端服务层、中间件层、基础底座层** + 技术选型说明表）<br>3.3.2 代码工程<br>3.3.2.1 工程模块（后端工程 + 前端工程，表格形式）<br>3.3.2.2 依赖关系（模块间关系图、单模块组成图、工程间依赖关系图 + 说明） |
| 3.4 数据架构 | 3.4.1 数据库访问设计<br>3.4.1.1 数据存储（数据存储介质、库、数据分类、数据产生、保留周期 - 表格形式）<br>3.4.1.2 读写方式（读写主库、读写分离、异构数据源访问策略）<br>3.4.2 分库分表设计（分库分表策略、路由规则、扩容方案） |

---

## 四、数据模型
必须包含以下**3个子章节**：

| 子章节 | 内容要求 |
|-------|---------|
| 4.1 概念模型 | **ER图 - 展示系统关键业务实体及关系** + 核心实体说明 |
| 4.2 逻辑模型 | **ER图 - 类似于领域模型，展示实体属性和详细关系** + 实体详情说明 |
| 4.3 物理模型 | 引用独立的《系统物理模型设计文档》，不内嵌详细表结构 |

---

## 五、接口设计
必须包含以下内容：

| 内容要求 |
|---------|
| 引用独立的《系统接口设计文档》，不内嵌详细接口定义 |

---

## 六、系统功能设计
必须包含以下**2个子章节**：

| 子章节 | 内容要求 |
|-------|---------|
| 6.1 业务功能设计 | 6.1.1 **主流程**（主流程时序图 + 说明）<br>6.1.2 按模块逐一展开，**每个模块下的每个完整功能作为一个子章节**，每个功能子章节包含：<br>　　- 功能概述<br>　　- 流程设计（流程图）<br>　　- 功能实现（核心类图 + 技术要点：异常处理、幂等、数据唯一性、并发写入、限流降级等） |
| 6.2 非业务功能设计 | **根据系统实际情况选择需要的非业务功能**，以下为常见场景示例：<br>　　- 集群高可用<br>　　- 分布式锁组件<br>　　- 节点高可用<br>　　- 配置管理组件<br>　　- 商户隔离<br>　　- WebRTC重连与统一弹屏等 |

---

## 七、附录
必须包含以下内容：

| 内容要求 |
|---------|
| - 相关文档链接<br>- 设计决策记录<br>- 变更历史 |

---

## 📐 系统物理模型设计文档标准
独立文档，章节结构：

| 章节 | 内容要求 |
|------|---------|
| 文档头 | 版本历史表（版本/状态、作者、变更日期、备注、审阅） |
| 按模块划分 | 每个模块下包含多张数据表定义：<br>**每张表使用统一表格结构，包含6列：字段、字段名称、类型(长度)、是否可空、默认值、说明**<br>　　- 业务字段定义<br>　　- 其他信息（create_time、update_time、delete_flag、tenant_id等通用字段）<br>　　- 索引（索引名称、字段、类型）<br>　　- 数据量级（数据增量/天）<br>　　- 归档规则（数据存储时效） |

### 🔴 物理模型生成流程（HARD RULE）

**生成物理模型文档必须遵循以下两阶段流程，禁止跳过 JSON 中间层直接写 Markdown：**

```
阶段1（结构化中间数据）：
  1. 读取 09-templates/系统物理模型设计文档-数据格式.json 理解数据 schema
  2. 按 JSON schema 结构生成完整物理模型数据：
     - domains[] → 按业务域分组
     - 每张表：fields[][]（6列数组）、indexes[][]（3列数组）、data_volume、data_retention
     - has_create_by / has_update_by / has_delete_flag 显式声明审计字段
     - appendix 含数据库全局规范、分库分表策略、数据同步规则
  3. 向用户展示 JSON 数据请确认，确认后进入阶段2

阶段2（Markdown 渲染 + DOCX 生成）：
  4. 读取 09-templates/系统物理模型设计文档模版.md 获取渲染模板
  5. 将阶段1的 JSON 数据按模板渲染为 Markdown 表格文档（供审阅）
  6. 🔴 DOCX 交付件必须使用 gen-physical-model.py 从 JSON 直接生成，禁止通过 pandoc 从 md 转换
     - 脚本路径: scripts/doc/gen-physical-model.py
     - 用法: python3 scripts/doc/gen-physical-model.py <input.json> <output.docx>
     - 原因: pandoc 转换丢失单元格合并、表头着色、字体设置；gen-physical-model.py 生成的 docx 格式与标准模板一致
```

**为什么需要 JSON 中间层**：Markdown 表格无法表达"其他信息""索引""数据量级""归档规则"等逻辑分隔行（相当于单元格合并语义），AI 直接写 Markdown 时会丢失层次关系。JSON 的嵌套结构天然表达这些层次，确保生成的 Markdown 表格结构正确。

---

## 📐 系统接口设计文档标准
独立文档，章节结构：

| 章节 | 内容要求 |
|------|---------|
| 文档头 | 版本历史表 |
| 通用约定 | - 请求头（Content-Type、Authorization、X-Request-ID等）<br>- 响应格式（成功响应、失败响应结构）<br>- 字符编码 |
| 通用响应结构 | 统一的 BaseResponse\<T\> 结构示例（JSON代码块格式） |
| 按模块划分 | 每个接口包含完整的5个子章节，顺序不可变：<br>1. 服务端点（基础URL + 环境说明：生产/测试/开发 + HTTP方法 + 接口路径）<br>2. 请求参数（表格形式：字段、类型、必填、说明、示例值）<br>3. 返回码说明（表格形式：code、message、说明、处理方案）<br>4. 数据字典（枚举值说明表格）<br>5. 范例请求（Request JSON示例 + Response JSON示例，代码块格式） |

---

## 🔍 设计原则应用要求
所有系统设计文档必须应用以下设计原则：

### 1. 高可用设计原则
- 无单点故障
- 故障自动转移
- 降级策略明确
- 多机房/多集群容灾

### 2. 数据一致性原则
- 最终一致性方案
- 幂等设计
- 分布式事务处理
- 补偿机制

### 3. 可扩展性原则
- 水平扩展支持
- 模块解耦
- 事件驱动架构
- 配置化设计

### 4. 性能设计原则
- 缓存策略（多级缓存、缓存预热、缓存失效）
- 异步处理
- 批量优化
- 数据库索引优化

---

## 📐 产出物验证清单
生成系统设计文档后请核对：

### 总体设计文档
- [ ] 7个大章节完整无缺
- [ ] 每个大章节的子章节完整无缺
- [ ] 所有架构图已嵌入（系统上下文、逻辑部署、**物理部署**、技术架构、数据模型）
- [ ] 技术架构图包含前端层、后端服务层、中间件层、基础底座层四层
- [ ] 设计原则章节标题根据系统特性自定义，不使用固定标题
- [ ] 业务功能设计包含主流程，且每个功能包含：功能概述、流程设计、功能实现（类图+技术要点）
- [ ] 非业务功能设计根据实际情况选择，不强制使用固定列表
- [ ] 概念模型展示关键业务实体及关系，逻辑模型为详细领域模型
- [ ] 所有表格格式规范、内容完整
- [ ] 质量属性指标已量化
- [ ] 子系统清单完整且职责清晰
- [ ] 数据存储矩阵表完整

### 物理模型设计文档
- [ ] 按模块划分表结构
- [ ] 每张表使用6列表格结构：字段、字段名称、类型(长度)、是否可空、默认值、说明
- [ ] 字段、其他信息、索引、数据量级、归档规则统一在表格内呈现
- [ ] 通用字段（id、create_time、update_time、delete_flag）齐全

### 接口设计文档
- [ ] 通用约定完整（请求头、响应格式）
- [ ] 统一响应结构为JSON代码块格式
- [ ] 每个接口的5个子项齐全且顺序正确
- [ ] 请求/响应JSON为代码块格式
- [ ] 返回码说明包含处理方案列

---

## 🔗 引用关系
```
系统设计文档（总体）
   ├── 引用 → 系统物理模型设计文档（独立文件）
   └── 引用 → 系统接口设计文档（独立文件）
```
